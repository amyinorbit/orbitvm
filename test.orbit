type Vector {
    var x: Number
    var y: Number
}

fun test(a: Number, b: Number)->Number {
    return a + b
}

fun main() {
    var test : Vector = make(Vector)
    test.x = 2
    var result = "Hello: " + (1234 + 45)
}
